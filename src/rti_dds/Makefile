######################################################################
#
# (c) Copyright, Real-Time Innovations, 2012.  All rights reserved.
# No duplications, whole or partial, manual or electronic, may be made
# without express written permission.  Any such copies, or
# revisions thereof, must display this notice unaltered.
# This code contains trade secrets of Real-Time Innovations, Inc.
#
#
# This makefile was automatically generated by rtiddsgen.
#
# To compile, type:
# 	make 
#
# Note: This makefile is only meant to build our example applications and
#       may require alterations to build on your system.
#
# This makefile assumes that your build environment is already correctly
# configured. (For example, the correct version of your compiler and
# linker should be on your PATH.)
######################################################################

TARGET_ARCH = i86Linux3gcc4.8.2

ifndef COMPILER
COMPILER = g++
endif
COMPILER_FLAGS = -m32 -Wall
ifndef LINKER
LINKER = g++
endif
LINKER_FLAGS = -m32 -static-libgcc -Wl,--no-as-needed
SYSLIBS = -ldl -lnsl -lm -lpthread -lrt
DEFINES = -DRTI_UNIX -DRTI_LINUX 
LIBFILES = objs/$(TARGET_ARCH)/rti_dds.o objs/$(TARGET_ARCH)/rti_ddsPlugin.o objs/$(TARGET_ARCH)/rti_ddsSupport.o objs/$(TARGET_ARCH)/ping_publisher.o objs/$(TARGET_ARCH)/ping_subscriber.o 


INCLUDES = -I. -I$(NDDSHOME)/include -I$(NDDSHOME)/include/ndds 
       
LIBS = -L$(NDDSHOME)/lib/$(TARGET_ARCH) \
        -lnddscppz -lnddscz -lnddscorez $(SYSLIBS)

CDRSOURCES    = rti_dds.idl
COMMONSOURCES = rti_ddsSupport.cxx rti_ddsPlugin.cxx rti_dds.cxx ping_publisher.cxx ping_subscriber.cxx

DIRECTORIES   = objs.dir objs/$(TARGET_ARCH).dir
COMMONOBJS    = $(COMMONSOURCES:%.cxx=objs/$(TARGET_ARCH)/%.o)

all: $(TARGET_ARCH) rti_dds.a

rti_dds:
	rtiddsgen -language C++ -example i86Linux3gcc4.8.2 rti_dds.idl

rti_dds.a: $(LIBFILES)
	ar rcs $@ $^

# We actually stick the objects in a sub directory to keep your directory clean.
$(TARGET_ARCH) : $(DIRECTORIES) $(COMMONOBJS) \
	$(EXEC:%=objs/$(TARGET_ARCH)/%.o) \
	$(EXEC:%=objs/$(TARGET_ARCH)/%)

objs/$(TARGET_ARCH)/% : objs/$(TARGET_ARCH)/%.o
	$(LINKER) $(LINKER_FLAGS)   -o $@ $@.o $(COMMONOBJS) $(LIBS)

objs/$(TARGET_ARCH)/%.o : %.cxx  rti_dds.h 
	$(COMPILER) $(COMPILER_FLAGS)  -o $@ $(DEFINES) $(INCLUDES) -c $<

#
# Uncomment these lines if you want the support files regenerated when idl
# file is modified
#
#
#  rti_ddsSupport.cxx rti_ddsPlugin.cxx rti_dds.cxx \
#  rti_ddsSupport.h rti_ddsPlugin.h rti_dds.h : \
#		rti_dds.idl
#	$(NDDSHOME)/bin/rtiddsgen rti_dds.idl -replace -language C++

# Here is how we create those subdirectories automatically.
%.dir : 
	@echo "Checking directory $*"
	@if [ ! -d $* ]; then \
		echo "Making directory $*"; \
		mkdir -p $* ; \
	fi;

.PHONY: all

